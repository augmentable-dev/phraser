kind: pipeline
name: default

workspace:
  base: /go
  path: src/github.com/augmentable-opensource/phraser

steps:
  - name: test
    image: golang:1.11.2
    secrets: [CC_TEST_REPORTER_ID]
    ports: [50060]

    commands:
      - curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
      - chmod +x ./cc-test-reporter
      - ./cc-test-reporter before-build
      - ./scripts/test
      # - ./cc-test-reporter format-coverage ./ -t c.out -o coverage-upload.txt -d
      # - ./cc-test-reporter upload-coverage -i coverage-upload.txt
      - echo $${CC_TEST_REPORTER_ID}
      - ./cc-test-reporter after-build -t gocov -r $${CC_TEST_REPORTER_ID}
